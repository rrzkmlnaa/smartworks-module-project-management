import { render } from '@testing-library/react'

import Departments from 'src/pages/modules/hrm/departments'

describe('Departments', () => {
  it('renders the Components', () => {
    render(<Departments />)
  })
})
