import { render } from '@testing-library/react'

import JobLevels from 'src/pages/modules/hrm/job-levels'

describe('Job Levels', () => {
  it('renders the Components', () => {
    render(<JobLevels />)
  })
})
